export enum ItemsRoutes {
  CATEGORIES = "/categories",
  BRANDS = "/brands"
}

export enum AppRoutes {
  ITEMS = "/items",
  SEARCH = "/search",
  ORDER = "/order",
  PROFILE = "/profile",
  PRODUCTS = "/products"
}